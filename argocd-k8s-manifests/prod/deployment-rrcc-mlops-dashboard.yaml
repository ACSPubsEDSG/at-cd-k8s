apiVersion: apps/v1
kind: Deployment
metadata:
  name: at-rrcc-mlops-dashboard
  namespace: advtech
spec:
  replicas: 1
  selector:
    matchLabels:
      app: at-rrcc-mlops-dashboard
  template:
    metadata:
      labels:
        app: at-rrcc-mlops-dashboard
    spec:
      containers:
      - name: at-rrcc-mlops-dashboard
        image: ghcr.io/acspubsedsg/at-rrcc-mlops-dashboard:prod
        imagePullPolicy: Always
        ports:
        - containerPort: 8080
        env:
        - name: SPRING_PROFILES_ACTIVE
          value: prod
        - name: TZ
          value: EST
        resources:
          requests:
            cpu: 1000m 
            memory: 2048Mi
          limits:
            cpu: 1500m 
            memory: 3072Mi
        # volumeMounts:
        # - mountPath: /appl/iapps/ssoProperties/
        #   name: at-rrcc-mlops-dashboard-config-volume
      # hostAliases:
      #   - hostnames:
      #       - "sso.acs.org"
      #     ip: "45.60.77.135"
      # volumes:
      #   - name: at-rrcc-mlops-dashboard-config-volume
      #     configMap:
      #       name: at-rrcc-mlops-dashboard-configmap
      imagePullSecrets:
        - name: ghcr-token-github-com