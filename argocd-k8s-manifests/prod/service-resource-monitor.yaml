apiVersion: v1
kind: Service
metadata:
  name: at-resource-monitor
  namespace: advtech
  annotations:
    cloud.google.com/backend-config: '{"ports": {"8080":"at-ai-fe-backendconfig"}}'
    cloud.google.com/neg: '{"ingress": true}'
  labels:
    app: at-resource-monitor
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: at-resource-monitor
    