apiVersion: v1
kind: Service
metadata:
  name: at-ai-admin-tool-dashboard
  namespace: advtech
  annotations:
    cloud.google.com/neg: '{"ingress": true}'
  labels:
    app: at-ai-admin-tool-dashboard
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: at-ai-admin-tool-dashboard
    