apiVersion: apps/v1
kind: Deployment
metadata:
  name: at-ai-aoe-fe-inferencing
  namespace: advtech
spec:
  replicas: 1
  selector:
    matchLabels:
      app: at-ai-aoe-fe-inferencing
  template:
    metadata:
      labels:
        app: at-ai-aoe-fe-inferencing
    spec:
      containers:
      - name: at-ai-aoe-fe-inferencing
        image: ghcr.io/acspubsedsg/at-ai-aoe-fe-inferencing:latest
        imagePullPolicy: Always
        env:
        - name: LD_LIBRARY_PATH
          value: /usr/local/nvidia/lib64
        ports:
          - containerPort: 5004
        resources:
          requests:
            cpu: 13200m
            memory: 293Gi
            nvidia.com/gpu: 1
          limits:
            cpu: 15500m
            memory: 293Gi
            nvidia.com/gpu: 1
        command:
          - /bin/sh
          - -c
          - mv aiamne-specter_stag.ini aiamne-specter.ini && python SimEvaluatorRealtime.py
        volumeMounts:
          - name: filestore
            mountPath: /NAS
          - mountPath: /usr/local/nvidia
            name: nvidia
      tolerations:
        - key: nvidia.com/gpu
          operator: Exists
          effect: NoSchedule
        - key: enable
          operator: "Equal"
          value: "highcpugpumemory"
          effect: NoSchedule
      imagePullSecrets:
        - name: ghcr-token-github-com
      volumes:
        - name: nvidia
          hostPath:
            path: /home/kubernetes/bin/nvidia
        - name: filestore
          persistentVolumeClaim:
            claimName: filestore-nfs-pvc
            readOnly: false