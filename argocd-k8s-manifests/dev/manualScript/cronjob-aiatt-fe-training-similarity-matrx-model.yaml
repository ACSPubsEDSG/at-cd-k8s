apiVersion: batch/v1
kind: CronJob
metadata:
  name: at-ai-att-fe-training-smlrtmtrxjob
  namespace: advtech
spec:
  schedule: "0 05 * * *"
  concurrencyPolicy: Forbid
  jobTemplate:
    spec:
      template:
        spec:
          containers:
          - name: at-ai-att-fe-training-smlrtmtrxjob
            image: ghcr.io/acspubsedsg/ai-att-training-be:latest
            imagePullPolicy: IfNotPresent
            resources:
              requests:
                cpu: 1000m 
                memory: 2048Mi
              limits:
                cpu: 1500m 
                memory: 3072Mi
            command:
            - /bin/sh
            - -c
            - mv config/manuscript-transfer-cnn_dev.ini config/manuscript-transfer-cnn.ini && conda run --name manuscript-transfer-cnn python jrn_top_n_2.py
            volumeMounts:
            - name: filestore 
              mountPath: /NAS
          imagePullSecrets:
          - name: ghcr-token-github-com
          volumes:
          - name: filestore
            persistentVolumeClaim:
              claimName: filestore-nfs-pvc
              readOnly: false
          restartPolicy: OnFailure